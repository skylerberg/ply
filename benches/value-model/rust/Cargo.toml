[package]
name = "kernels"
version = "0.1.0"
edition = "2024"
publish = false

# ADR 0035's bars: no dependencies, so the kernels are exactly what is written here.
[dependencies]

[profile.release]
opt-level = 3
debug = false

[workspace]
