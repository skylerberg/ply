import fieldorder (slow, fast, bare)

// A `ply run` entry point so this directory is a project `ply test` accepts.
// The measurements live in `bench.sh`, which rewrites this file in a temporary
// directory rather than editing it here.
fn main() -> Int = fast(1000)
