// `handle` with a resource, a `resume` binder, several params, and a `return`
// clause — `ReturnClause` is `rcl`.
fn f() = handle g() with {
  db.read[users]() -> 1,
  db.put(k, v) resume r -> r(0),
  return x -> x + 1,
}
