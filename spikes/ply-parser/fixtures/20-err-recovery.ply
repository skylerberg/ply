// `recover_to_item`, which no real `.ply` file in the tree reaches one line of:
// garbage between items, an abandoned item, and a bracket depth that has to be
// carried past an item keyword before resynchronising.
fn a() = 1
$$$
fn b() = 2
type T = 9 ( fn ) 
fn c() = 3
