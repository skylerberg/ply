// A list pattern's `rest` is an `Option<Box<Pattern>>`: present, absent, and
// nested inside another list pattern.
fn f(v) = match v {
  [] -> 0,
  [a] -> a,
  [a, ..rest] -> a,
  [[x, ..inner], ..outer] -> x,
}
