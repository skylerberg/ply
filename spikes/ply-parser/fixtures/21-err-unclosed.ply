// Unclosed delimiters: each labels its opener as a secondary as well as the
// offending token as primary, so this fixture watches label *count* and each
// label's own span, not just the diagnostic code.
fn f(a: Int
