// E0412: a deterministic test whose footprint still reaches the clock.
nondet effect clock {
  read now() -> Int
}

fn stamp() -> Int / {clock.read} = clock.now()

test "a stamp is recent" {
  assert(stamp() > 0)
}
