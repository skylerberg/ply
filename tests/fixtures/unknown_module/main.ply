// E0106: `orders` is never imported in this file, so `orders::` names no
// module. A qualified name only ever resolves through an `import` in the file
// that writes it.
fn total() -> Int =
  orders::place()
