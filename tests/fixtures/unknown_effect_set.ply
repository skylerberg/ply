// E0114: a set is module-local, so `shared::Web` names nothing this file can
// reach, and `db` is an effect rather than a set — a member is always an atom.
effect db {
  read all[t]() -> Int
}

effect set Web = {db, db.read[users]}

fn report() -> Int / {shared::Web} = db.all[users]()
