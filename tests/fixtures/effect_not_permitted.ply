// E0302: the body writes, and the published signature only permits reads.
effect db {
  read  all[table]() -> List<Int>
  write save[table](rows: List<Int>) -> Unit
}

fn compact() -> Int / {db.read[orders]} {
  let rows = db.all[orders]();
  db.save[orders]([]);
  len(rows)
}
