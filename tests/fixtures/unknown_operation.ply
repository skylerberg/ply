// E0104: `db` declares `all` and `save`, but not `get`.
effect db {
  read  all[table]() -> List<Int>
  write save[table](rows: List<Int>) -> Unit
}

fn first_user() -> Int / {db.read[users]} =
  db.get[users](1)
