// E0108: `place` is both imported and defined here. Letting the local win
// would silently steal every existing call site, so it is an error; write
// `orders::place()` or `import orders as ord` to say which one is meant.
import orders (place)

fn place() -> Int = 2

fn total() -> Int =
  place()
