// E0301: the row variable `e` is never declared, so the signature promises an
// effect nobody can supply. It belongs in the generic list: `fn run<| e>(..)`.
fn run(body: () -> Int) -> Int / {| e} = body()
