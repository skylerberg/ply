// E0115: expansion is a fixed point, and `Web -> Storage -> Web` has none.
effect db {
  read all[t]() -> Int
}

effect set Web = {Storage, db.read[users]}
effect set Storage = {Web}

fn report() -> Int / {Web} = db.all[users]()
