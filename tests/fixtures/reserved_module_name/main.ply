// The importer, so the fixture is a program rather than a lone file: without
// the rule above, which of the two `std.net` modules this names would depend on
// where the project root happens to be.
import std.net (listen)

fn main() -> Int = listen(8080)
