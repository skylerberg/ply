// E0117: a record update replaces fields, it does not add them. The result's
// type is the base's type, so a name the base does not have is a field nothing
// would ever read.
type Limits = { max_body: Int, max_headers: Int }

fn typo(l: Limits) -> Limits = {..l, max_bodys: 1}
