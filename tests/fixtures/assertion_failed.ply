// E0501 at run time: the expected and actual values differ.
fn running(xs: List<Int>) -> Int = fold(xs, 0, |acc, x| acc + x)

test "the running total of the journal" {
  assert_eq(running([500000, -120000, 90000]), 480000)
}
