// `TypeExpr::Unit` in a parameter, a return, a variant field and a function
// type; and the parenthesised type whose span `ty_body` does *not* rewrite.
fn f(x: ()) -> () = ()
fn g(h: (()) -> ()) -> (Int) = h(())
type T = | A(()) | B((Int))
