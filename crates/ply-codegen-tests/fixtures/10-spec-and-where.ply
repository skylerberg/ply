// `requires`/`ensures` in both orders and a `where` with two constraints.
fn f<a, b>(x: a, y: b) -> Int
  where derivable(json, a), derivable(ord, b)
  requires true
  ensures result >= 0
  = 0
fn g(n: Int) -> Int ensures result > n requires n > 0 = n + 1
