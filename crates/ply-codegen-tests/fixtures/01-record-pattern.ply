// `PatternKind::Record`, the one node tag neither the twenty-one real files nor
// the mined reference tests reach. Shorthand, `:`-bound, nested, and `..`.
fn f(v) = match v {
  { x, y } -> x,
  { x: 1, y: { z, .. } } -> z,
  { .. } -> 0,
}
