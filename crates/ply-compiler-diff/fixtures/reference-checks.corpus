Generated by spikes/ply-parser/mine-checks.py from
crates/ply-core/src/tests.rs. Do not edit; regenerate.

Everything above the first `%%` is this header. Below it, fixtures are
separated by a line holding exactly `%%`, and the newline before a
separator belongs to the separator: a fixture that ends in a newline is
written with two, and one that does not is written with one.
%%
net
%%
fetch
%%
Int
%%
expected success, got: {}
%%
expected failure, but the module checked
%%
  @{}..{} {}
%%
[{}] {}
{}
%%


%%
no definition `{name}`
%%
no `{code}` diagnostic in:
{}
%%
db
%%
get
%%
put
%%
Unit
%%
wall
%%
now
%%
id
%%
x
%%
a
%%
<a>(a) -> a
%%
{}
%%
f
%%
main
%%
the signature is written: the mistake under test is the second instantiation, not a missing annotation:
{}
%%
twice
%%
bad
%%
read_one
%%
users
%%
{db.read[users]}
%%
() -> Int / {db.read[users]}
%%
reader
%%
writer
%%
orders
%%
both
%%
{db.write[orders], db.read[users]}
%%
apply
%%
e
%%
use_it
%%
<a | e>(() -> a / e) -> a / e
%%
doubled
%%
xs
%%
map
%%
List
%%
(List<Int>) -> List<Int>
%%
isolated
%%
k
%%
resumed
%%
kont
%%
() -> Int
%%
handled
%%
{net.read}
%%
int_to_string
%%
String
%%
aborted
%%
() -> String
%%
still_dirty
%%
{db.read[orders]}
%%
backed_by_socket
%%
cell_set
%%
c
%%
cell_get
%%
counted
%%
leaked
%%
escaped
%%
the cell escapes its `with_cell[users]` region
%%
Cell[users]
%%
held
%%
g
%%
_ignored
%%
h
%%
outer
%%
inner
%%
d
%%
nested
%%
confused
%%
sensors
%%
read_at
%%
north
%%
south
%%
wrong resource
%%
sensors.read[north]
%%
sensors.read_at[north]()
%%
{:?}
%%
one handled one not
%%
the blamed perform must be the surviving one
%%
peek
%%
Cell
%%
reads_only
%%
audit
%%
{db.write[audit], db.read[users]}
%%
sneaky
%%
db.write[orders]
%%
assert
%%
uses the clock
%%
nondeterministic effect in a deterministic test
%%
wall.read
%%
nondet
%%
wall.now()
%%
expected a handler suggestion, got {:?}
%%
test/nondet
%%
expected the opt-out suggestion, got {:?}
%%
frozen clock
%%
wall clock
%%
{wall.read}
%%
stamp
%%
indirect
%%
calls
%%
db.get[
%%
nope
%%
b
%%
db.get
%%
missing_one
%%
missing_two
%%
Bool
%%
is_even
%%
n
%%
is_odd
%%
(Int) -> Bool
%%
looper
%%
caller
%%
later
%%
o
%%
None
%%
Some
%%
v
%%
unwrap_or_zero
%%
Option
%%
wrap
%%
(Option<Int>) -> Int
%%
() -> Option<Int>
%%
partial
%%
omega
%%
Wrapper
%%
Tag
%%
`Tag` is defined twice
%%
a real span
%%
the first declaration has to be pointed at too: {}
%%
rename
%%
as a function
%%
thing
%%
cell
%%
Count
%%
bump
%%
(Int) -> Int
%%
Loop
%%
store
%%
echo
%%
as_int
%%
as_bool
%%
() -> Int / {store.read[k]}
%%
() -> Bool / {store.read[k]}
%%
reads users
%%
pure
%%
forwarding
%%
the clause body is what still performs it
%%
len
%%
() -> Bool
%%
r
%%
(List<Int>) -> Int
%%
the empty list
%%
not covered: lists longer than 1
%%
y
%%
z
%%
not covered: lists of 1 element, lists of 2 elements
%%
first
%%
second
%%
p
%%
({first: Int, second: Int}) -> Int
%%
record pattern does not name every field
%%
`second`
%%
cmp
%%
functions cannot be compared for equality
%%
run
%%
t
%%
<a>(a, a) -> Bool
%%
`{name}` should parse: {}
%%
expected every `derive` to expand: {}
%%
expected resolution to succeed: {}
%%
expected failure, but the program checked
%%
base
%%
pub fn one() -> Int = 1
%%
middle
%%
import base (one)
pub fn two() -> Int = one() + one()
%%
top
%%
import middle
fn four() -> Int = middle::two() + middle::two()
%%
base.one
%%
middle.two
%%
top.four
%%
four
%%
left
%%
import base (one)
pub fn l() -> Int = one()
%%
right
%%
import base
pub fn r() -> Int = base::one()
%%
import left
import right
fn t() -> Int = left::l() + right::r()
%%
top.t
%%
fn secret() -> Int = 1
pub fn place() -> Int = secret()
%%
app
%%
import store
fn f() -> Int = store::secret()
%%
private to module `store`
%%
pub fn secret
%%
the fix must name the module that would have to export it: {:?}
%%
import b
pub fn f() -> Int = b::g()
%%
import a
pub fn g() -> Int = a::f()
%%
`a` -> `b` -> `a`
%%
pub fn place() -> Int = 1
%%
import store (place)
fn place() -> Int = 2
%%
fn len(x: Int) -> Int = x + 1
fn shadowed(len: Int) -> Int = len
fn module_item() -> Int = len(1)
%%
app.shadowed
%%
app.module_item
%%
fn count(xs: List<Int>) -> Int = len(xs)
%%
app.count
%%
pub fn place() -> Int = 7
%%
import orders
fn f(orders: Int) -> Int = orders + orders::place()
%%
app.f
%%
pub effect db { read get[r](key: Int) -> Int }
pub fn read_one() -> Int = db.get[users](1)
%%
left.read_one
%%
{left.db.read[users]}
%%
right.read_one
%%
{right.db.read[users]}
%%
pub effect db { read get[r](key: Int) -> Int
  write put[r](key: Int, value: Int) -> Unit }
%%
import store (db)
pub fn r() -> Int = db.get[users](1)
%%
import store (db)
pub fn w() -> Unit = db.put[users](1, 2)
%%
reader.r
%%
{store.db.read[users]}
%%
writer.w
%%
{store.db.write[users]}
%%
a resource label is a claim about the world, not about a file
%%
pub effect db { read get[r](key: Int) -> Int }
%%
import store
fn reads() -> Int = store::db.get[users](1)
fn handled() -> Int = handle reads() with { store::db.get[users](k) -> k }
%%
app.reads
%%
app.handled
%%
shapes
%%
pub type Shape = Circle(Int) | Square(Int)
%%
import shapes
fn make() -> shapes::Shape = shapes::Circle(2)
fn area(s: shapes::Shape) -> Int = match s { shapes::Circle(r) -> r * r, shapes::Square(w) -> w * w }
%%
app.make
%%
() -> shapes.Shape
%%
app.area
%%
(shapes.Shape) -> Int
%%
shapes.Circle
%%
type Shape = Circle(Int)
%%
import shapes
fn f(s: Int) -> Int = match s { shapes::Circle(r) -> r, _ -> 0 }
%%
money
%%
type Cents = Int
pub type Money = Cents
%%
import money
fn total(m: money::Money) -> Int = m + 1
%%
app.total
%%
fn f() -> Int = store::place()
%%
import store
%%
fn f() -> Int = place()
%%
import store (place)
%%
test "it works" { assert(true) }
%%
left.it works
%%
right.it works
%%
clock
%%
pub nondet effect clock { read now() -> Int }
%%
import clock
test "reads the clock" { assert(clock::clock.now() > 0) }
%%
clock::clock.now()
%%
timing
%%
import timing (clock)
test "reads" { assert(clock.now() > 0) }
%%
{ clock.now() -> <value> }
%%
pub fn twice(x: Int) -> Int = x + x
%%
left.twice
%%
right.twice
%%

%%
mine
%%
pub fn len(x: Int) -> Int = x
%%
import mine (len)
fn f() -> Int = len(3)
%%
pub type Shape = Circle(Int)
%%
import shapes
fn f() -> Int = shapes::Shape
%%
has no definition `Shape`
%%
`Circle`
%%
CARGO_MANIFEST_DIR
%%
../../examples
%%
the example corpus is part of the repository
%%
ply
%%
example is readable
%%
every definition is keyed by its program-wide name
%%
a test key is `<module>.<label>`, which is what keeps two labels apart
%%
task
%%
spawn
%%
join
%%
() -> Int / {sim.read}
%%
yield
%%
s
%%
sleep
%%
random
%%
next
%%
below
%%
sim
%%
seed
%%
() -> Unit / {task.write}
%%
() -> Int / {clock.read}
%%
() -> Unit / {clock.write}
%%
() -> Int / {random.write}
%%
{db.write[orders], task.write}
%%
{db.write[north], db.write[south], task.write}
%%
{sim.read}
%%
{sim.read, wall.read}
%%
clock.read
%%
clock.write
%%
random.write
%%
task.write
%%
unscheduled
%%
simulate { <body> }
%%
the language ships the handler, so that is what to point at: {:?}
%%
reads the wall
%%
simulated
%%
half simulated
%%
Task
%%
the nested region is named too: {:?}
%%
the transitive case says so: {:?}
%%
pinned seed
%%
sequential scheduler
%%
go
%%
prelude
%%
nondet effect clock { read now() -> Int }
fn f() -> Int = clock.now()

%%
clock.f
%%
{clock.clock.read}
%%
m
%%
GET
%%
Bytes
%%
() -> Bytes
%%
string_concat
%%
bytes_len
%%
(Bytes) -> Int
%%
bytes_at
%%
(Bytes, Int) -> Int
%%
bytes_slice
%%
(Bytes, Int, Int) -> Bytes
%%
bytes_concat
%%
(Bytes, Bytes) -> Bytes
%%
bytes_concat_all
%%
(List<Bytes>) -> Bytes
%%
bytes_of_string
%%
(String) -> Bytes
%%
bytes_is_utf8
%%
(Bytes) -> Bool
%%
bytes_index_of
%%
(Bytes, Bytes) -> Option<Int>
%%
bytes_index_of_from
%%
(Bytes, Bytes, Int) -> Option<Int>
%%
bytes_index_of_byte
%%
(Bytes, Int) -> Option<Int>
%%
bytes_starts_with
%%
(Bytes, Bytes) -> Bool
%%
bytes_ends_with
%%
bytes_split
%%
(Bytes, Bytes) -> List<Bytes>
%%
bytes_scan
%%
(Bytes, Int, Bytes, Int) -> Int
%%
bytes_scan_until
%%
string_of_bytes
%%
(Bytes) -> String
%%
string_of_bytes_lossy
%%
string_len
%%
(String) -> Int
%%
string_slice
%%
(String, Int, Int) -> String
%%
string_split
%%
(String, String) -> List<String>
%%
string_trim
%%
(String) -> String
%%
string_lower
%%
string_upper
%%
string_starts_with
%%
(String, String) -> Bool
%%
string_ends_with
%%
string_contains
%%
string_find
%%
(String, String) -> Int
%%
fn probe_{name}() -> {ty} = {name}

%%
probe_{name}
%%
() -> {ty}
%%
{name}
%%
builtin type
%%
through a call
%%
../../tests/fixtures
%%
unscheduled_task.ply
%%
task_escapes_scope.ply
%%
nested_simulation.ply
%%
`{file}` is part of the repository: {e}
%%
.ply
%%
should parse: {}
%%
effect db {
  read get[r](k: Int) -> Int
  write put[r](k: Int, v: Int) -> Unit
}

%%
fn withdraw(a: Int, n: Int) -> Int
requires n > 0
ensures result == a - n
requires n <= a
ensures result <= a
= a - n
%%
withdraw
%%
requires
%%
ensures
%%
{DB}fn store(k: Int) -> Unit = db.put[users](k, 1)
%%
{DB}fn store(k: Int) -> Unit requires k > 0 ensures k > 0 = db.put[users](k, 1)
%%
{db.write[users]}
%%
{DB}fn store(k: Int) -> Unit requires k > 0 = db.put[users](k, 1)
test "stores" {{ handle {{ store(1) }} with {{ db.put[users](k, v) -> () }} }}
%%
a `requires` clause
%%
an `ensures` clause
%%
{DB}fn f(k: Int) -> Int {clause} db.get[users](k) > 0 = k
%%
db.read[users]
%%
post-state it caused
%%
fn f() -> Int {clause} (simulate {{ true }}) = 1
%%
sim.read
%%
fn g<a | e>(f: () -> Bool / e) -> Int requires f() = 1
%%
not known to be empty
%%
fn f(x: Int) -> Int requires x = x
%%
fn f(x: Int) -> Int ensures result >= x = x + 1
%%
fn f(x: Int) -> Int requires result > 0 = x
%%
bound only in an `ensures`
%%
fn f(result: Int) -> Int = result
%%
fn f(result: Int) -> Int ensures result > 0 = result
%%
fn credited(a: Int, n: Int) -> Int = a + n
fn debited(a: Int, n: Int) -> Int = a - n
law "credit and debit cancel" forall (a: Int, n: Int) where n > 0 && n <= a {
credited(debited(a, n), n) == a
}
%%
credit and debit cancel
%%
{}: {}
%%
a: Int
%%
n: Int
%%
law "one is one" { 1 == 1 }
%%
law "f is a function" forall (f: (Int) -> Int, x: Int) { f(x) == f(x) }
%%
law "length agrees" forall (xs: List<a>) { len(xs) == len(xs) }
%%
List<a>
%%
law "pins a" forall (x: a) { x == 1 }
%%
law "conserves" forall (n: Int) { simulate { n == n } }
%%
law "seeded guard" forall (n: Int) where (simulate { n > 0 }) { n == n }
%%
`where`
%%
{DB}law "reads" forall (n: Int) {{ db.get[users](n) == n }}
%%
law body
%%
law/host
%%
the refusal must name the fix: {}
%%
{DB}law/host "reads" forall (n: Int) {{ db.get[users](n) == n }}
%%
{DB}law/host "guarded" forall (n: Int) where db.get[users](n) > 0 {{ n == n }}
%%
law "any handler" forall (h: Handler<Int>) { true }
%%
law "any handler" forall (h: handler) { true }
%%
a handler
%%
handler-parametric law
%%
law "c" forall (c: Cell<Int>) { true }
%%
with_cell
%%
law "t" forall (t: Task<Int>) { true }
%%
scheduler
%%
type Box = Wrap(Cell<Int>)
law "b" forall (b: Box) { true }
%%
{src}: {}
%%
{DB}law "f" forall (f: (Int) -> Int / {{db.read[users]}}) {{ true }}
%%
law "f" forall (f: (Int) -> Int / e) { true }
%%
effect variable
%%
law "same" { true }
law "same" { false }
%%
law "first" { true }
law "second" { true }
%%
law "third" { true }
%%
a.first
%%
a.second
%%
b.third
%%
fn withdraw(a: Int, n: Int) -> Int requires n > 0 ensures result == a - n = a - n
%%
ledger
%%
resolves
%%
checks
%%
checks from interfaces
%%
ledger.withdraw
%%
fn withdraw(a: Int, n: Int) -> Int ensures result == "x" = a - n
%%
a clause comparing Int to String is a mismatch
%%
the corpus carries no `requires` or `ensures`
%%
the corpus carries no law
%%
`{}` clause #{} has footprint {}
%%
law {:?} has footprint {}
%%
the corpus demonstrates no concurrency law
%%
the corpus demonstrates no `law/host`
%%
fn f(x: Int) -> Int ensures result > x = x + 1
%%
fn f(x: Int) -> Int ensures result == "grown" = x + 1
%%
fn even(n: Int) -> Bool requires n >= 0 = if n == 0 { true } else { odd(n - 1) }
fn odd(n: Int) -> Bool ensures result == !even(n) = if n == 0 { false } else { even(n - 1) }
%%
even
%%
odd
%%
pub fn twice(n: Int) -> Int = n * 2
%%
import base (twice)
fn quad(n: Int) -> Int ensures result == twice(twice(n)) = twice(twice(n))
law "quadrupling is doubling twice" forall (n: Int) {
quad(n) == twice(twice(n))
}
%%
app.quad
%%
app.quadrupling is doubling twice
%%
effect s { read g[r]() -> Int }
fn balance() -> Int = handle { s.g[r]() } with { s.g[r]() -> 1 }
fn balance(o: Int) -> Int = o

%%
the checker did not report the duplicate: {}
%%
fn f(a: Int, b: Int = "not an int") -> Int = a + b
fn one() -> Int = f(1)
fn two() -> Int = f(2)
fn three() -> Int = f(3)

%%
expected the default's own error and one for the spliced copies, got {:#?}
%%
{diags:#?}
%%
{src}fn four() -> Int = f(4)

%%
{more:#?}
